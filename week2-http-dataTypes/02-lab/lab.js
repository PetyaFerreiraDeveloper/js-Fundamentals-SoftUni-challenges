// let x = 10 / 'p';
// if (x) {
//     console.log('True')
// } else {
//     console.log('False')
// }


let y = 2;
console.log(Boolean(y));

null == undefined // true
null === undefined // false
null !== undefined // true

// undefined
// We create a variable, but we don't assign it a value. This variable is undefined, and it doesn't take space from the memory

// null
// we create a variable with a value of null. The variable exists, it takes space in the memory, but there is nothing in it

